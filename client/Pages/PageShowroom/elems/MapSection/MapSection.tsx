import React, { FC, memo } from 'react';

import styles from './SectionShowroomsMap.module.css';

const MapSection: FC = () => {
  return <div>Лучше один раз увидеть!</div>;
};

export default memo(MapSection);
