import React, { FC, memo } from 'react';

import styles from './CurrentPromotions.module.css';

const CurrentPromotions: FC = () => {
  return <div>Действующие акции</div>;
};

export default memo(CurrentPromotions);
