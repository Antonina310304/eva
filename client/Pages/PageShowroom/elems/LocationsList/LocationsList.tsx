import React, { FC, memo } from 'react';

import styles from './LocationsList.module.css';

const LocationsList: FC = () => {
  return <div>Список магазинов</div>;
};

export default memo(LocationsList);
