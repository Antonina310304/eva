import React, { FC, memo } from 'react';

import styles from './LocationsSlider.module.css';

const LocationsSlider: FC = () => {
  return <div>Магазины Диван.ру</div>;
};

export default memo(LocationsSlider);
