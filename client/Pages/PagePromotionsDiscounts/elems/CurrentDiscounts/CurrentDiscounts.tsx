import React, { FC, memo } from 'react';

import styles from './CurrentDiscounts.module.css';

const CurrentDiscounts: FC = () => {
  return <div>Актуальные скидки</div>;
};

export default memo(CurrentDiscounts);
