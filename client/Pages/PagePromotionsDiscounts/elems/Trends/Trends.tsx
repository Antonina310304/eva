import React, { FC, memo } from 'react';

import styles from './Trends.module.css';

const Trends: FC = () => {
  return <div>Будь в тренде: яркий интерьер в деталях</div>;
};

export default memo(Trends);
